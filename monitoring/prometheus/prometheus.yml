global:
  scrape_interval: 5s
  evaluation_interval: 5s

scrape_configs:
  - job_name: "filtering"
    metrics_path: /metrics
    static_configs:
      - targets: ["filtering:8000"]

  - job_name: "localllm"
    metrics_path: /metrics
    static_configs:
      - targets: ["localllm:8001"]
