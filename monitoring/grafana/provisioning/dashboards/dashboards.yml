apiVersion: 1

providers:
  - name: "Prompt Filter Dashboards"
    orgId: 1
    folder: "Prompt Filter"
    type: file
    disableDeletion: false
    editable: true
    updateIntervalSeconds: 10
    options:
      path: /var/lib/grafana/dashboards
